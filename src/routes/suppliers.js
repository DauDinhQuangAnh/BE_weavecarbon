// Compatibility export: the stable route import remains unchanged while the
// implementation lives in the suppliers/compliance reference module.
module.exports = require('../modules/suppliers-compliance');
